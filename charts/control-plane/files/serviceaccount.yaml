apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "scp.serviceAccountName" . }}
  labels:
    {{- include "scp.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
