apiVersion: v1
kind: Service
metadata:
  name: {{ include "cp.fullname" . }}
  labels:
    {{- include "cp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cp.selectorLabels" . | nindent 4 }}
