apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.contentsSecret.name }}
  labels:
    {{- include "scp.labels" $ | nindent 4 }}
type: Opaque
stringData:
  {{- range $systemKey, $systemVal := .Values.config.systems }}
  {{- if $systemVal.enabled }}
  {{- range $secretKey, $secretVal := dict "systemUserCreds" "sys-user.creds" "operatorSigningKey" "operator-sk.nk" }}
  {{- $secret := get $systemVal $secretKey }}
  {{- if and $secret $secret.contents }}
  "{{ $systemKey }}.{{ $secretVal }}": {{ $secret.contents | quote }}
  {{- end }}
  {{- end }}
  {{- end }}
  {{- end }}
