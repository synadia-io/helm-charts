---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "cp.fullname" . }}
  labels:
    {{- include "cp.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  {{- if or (include "cp.defaultEncryptionKey" .) (or .Values.embeddedPostgres.persistent .Values.embeddedPrometheus.persistent) }}
  strategy:
    type: Recreate
  {{- end }}
  selector:
    matchLabels:
      {{- include "cp.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/config: {{ .configmapChecksum }}
        checksum/secrets: {{ .secretsChecksum }}
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "cp.selectorLabels" . | nindent 8 }}
    spec:
      {{- $imagePullSecrets := fromJson (include "cp.imagePullSecrets" .)}}
      {{- with $imagePullSecrets.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "cp.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args:
            - -c
            - /conf/rna.cue
            - server
            - start
          volumeMounts:
            - name: config
              subPath: rna.cue
              mountPath: "/conf/rna.cue"
            {{- if (include "cp.defaultEncryptionKey" .) }}
            - name: {{ include "cp.fullname" . }}
              mountPath: {{ get .config "data_dir" | default "/app/data" }}/encryption
              subPath: encryption
            {{- end }}
            {{- if and .Values.embeddedPostgres.persistent .postgresEnabled}}
            - name: postgres
              mountPath: {{ get .config "data_dir" | default "/app/data" }}/postgres
            {{- end }}
            {{- if and .Values.embeddedPrometheus.persistent .prometheusEnabled }}
            - name: prometheus
              mountPath: {{ get .config "data_dir" | default "/app/data" }}/prometheus
            {{- end }}
            {{- range $system, $secretName := .secretNames }}
            - name: "{{ $secretName }}"
              mountPath: "/conf/helix/nsc/{{ $system }}"
            {{- end }}
          ports:
            - name: http
              containerPort: {{ get .config "http_public_addr" | splitList ":" | last | default 8080 }}
              protocol: TCP
            - name: metrics
              containerPort: {{ get .config "http_metrics_addr" | splitList ":" | last | default 7777 }}
              protocol: TCP
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      volumes:
        - name: config
          configMap:
            name: "{{ include "cp.fullname" . }}-config"
        {{- if or (not (hasKey .config "encryption_key")) (eq (get .config "encryption_key") "") }}
        - name: {{ include "cp.fullname" . }}
          persistentVolumeClaim:
            claimName: {{ include "cp.fullname" . }}
        {{- end }}
        {{- if and .Values.embeddedPostgres.persistent .postgresEnabled }}
        - name: postgres
          persistentVolumeClaim:
            claimName: "{{ include "cp.fullname" . }}-postgres"
        {{- end }}
        {{- if and .Values.embeddedPrometheus.persistent .prometheusEnabled }}
        - name: prometheus
          persistentVolumeClaim:
            claimName: "{{ include "cp.fullname" . }}-prometheus"
        {{- end }}
        {{range $system, $secretName := .secretNames }}
        - name: "{{ $secretName }}"
          secret:
            secretName: {{ $secretName }}
        {{- end }}