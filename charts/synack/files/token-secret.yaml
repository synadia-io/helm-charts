apiVersion: v1
kind: Secret
metadata:
  {{- include "synack.metadataNamespace" $ | nindent 2 }}
  name: {{ .Values.tokenSecret.name }}
  labels:
    {{- include "synack.labels" $ | nindent 4 }}
type: Opaque
stringData:
  token: {{ .Values.config.token }}
